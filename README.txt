Demo scripts are in the folder src/demo/*

The whole package should be on the MATLAB path (src and lib have to be the subfolders).

External code:
* http://www.uv.es/gcamps/code/graph.htm | Used for comparison, no functional uses.
* https://github.com/mli/nystrom | Used for reference
* http://www.mathworks.com/matlabcentral/fileexchange/38422-improved-nystrom-kernel-low-rank-approximation | Used for reference

The file `mapPredict3.mat` in `sourcecode` contains a very good classification result.

Git commits were all done by Yinglai, because he knows it.